/* struct point{
    int x;
    int y;
}; // 구조체 정의 (붕어빵 틀)
struct point p1; // 구조체 변수 선언 (붕어빵 반죽) 
//point = 구조체 태그(구조체 이름)
//p1 = 구조체 변수 이름
// x, y = 구조체 멤버

p1.x = 10; // 구조체 멤버 접근
p1.y = 20; // 구조체 멤버 접근
*/

