// const double PI = 3.14159; 상수 선언
// PI = x; PI는 변경 할 수 없으므로 오류 발생
// cin >> PI; PI에 값을 넣을 수 없으므로 오류 발생
// cout << PI; PI의 값을 화면에 출력
// x = PI; PI의 값을 다른 변수에 할당

// perimeter = 2 * PI * radius; // 2는 리터럴(상수 그대로 사용), PI는 상수

